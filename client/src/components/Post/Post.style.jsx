import { makeStyles } from "@mui/styles";

const localStyles = makeStyles(() => ({
    wrapper: {
        padding: '1rem',
        minHeight: '50vh',
    },
    title: {
        textAlign: 'center',
    },
}));

export default localStyles;