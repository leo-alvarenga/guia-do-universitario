import { makeStyles } from "@mui/styles";

const localStyles = makeStyles(() => ({
    wrapper: {
        padding: '1rem',
        minHeight: '50vh',
    },
    title: {
        textAlign: 'center',
    },
    cover: {
    },
    body: {
        margin: '5rem 0 0 0',
    },
}));

export default localStyles;